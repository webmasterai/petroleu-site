# Local development (Vite proxies /api → local CMS on 127.0.0.1:8001)
VITE_CMS_API_BASE_URL=/api/cms

# Coolify production build-arg (absolute URL to THIS site's CMS backend):
# VITE_CMS_API_BASE_URL=https://<CMS-BACKEND-DOMAIN>/api/cms
